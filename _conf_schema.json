{
  "interval_hours": {
    "type": "float",
    "description": "自动统计间隔（小时）",
    "hint": "超过此时间未使用/token，下一次AI回复后会自动输出统计信息",
    "default": 24.0,
    "slider": {
      "min": 1,
      "max": 720,
      "step": 1
    }
  },
  "session_ttl_hours": {
    "type": "float",
    "description": "会话过期时间（小时）",
    "hint": "会话无活动超过此时间会被自动清理，避免内存泄漏",
    "default": 72.0,
    "slider": {
      "min": 1,
      "max": 720,
      "step": 1
    }
  },
  "persist_enabled": {
    "type": "bool",
    "description": "是否启用本地会话统计持久化",
    "hint": "开启后会将统计数据保存到 AstrBot 标准数据目录，重启后可恢复",
    "default": true
  },
  "persist_interval_minutes": {
    "type": "int",
    "description": "持久化保存间隔（分钟）",
    "hint": "建议 5 分钟，可根据需求调整至更低（如 1 分钟）以减少数据丢失风险",
    "default": 5.0,
    "minimum": 1,
    "maximum": 60
  }
}